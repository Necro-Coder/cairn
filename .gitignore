# ---------------------------------------------------------------------------
# Local tooling. Every contributor uses different editors and helpers, and none
# of that configuration belongs to anyone else. Dot directories at the root are
# ignored by default; the two that are genuinely part of the project are let
# back in explicitly.
# ---------------------------------------------------------------------------
/.*/
!/.cargo/
!/.github/
!/.githooks/
*.code-workspace

# Personal working notes, kept next to the code but never published.
/private/
*.private.md
*.local.md
*.local.toml
*.local.json

# ---------------------------------------------------------------------------
# Secrets and credentials. Nothing here ever reaches the repository.
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.mobileprovision
*.p8
*.cer
*.certSigningRequest
*.keystore
*.jks
secrets/
*.pairing
*.plist.local

# ---------------------------------------------------------------------------
# Rust
# ---------------------------------------------------------------------------
/target/
**/target/
*.pdb
*.rs.bk
# Cargo.lock IS committed: builds must be reproducible (--locked).

# ---------------------------------------------------------------------------
# Node / frontend
# ---------------------------------------------------------------------------
node_modules/
dist/
dist-ssr/
# The preview build, which must never be mistaken for the real bundle and must never be
# published anywhere.
dist-preview/
*.tsbuildinfo

# What the accessibility gate leaves behind: a report, and a trace for anything that
# failed. Both are rebuilt by every run and neither belongs in the history.
test-results/
playwright-report/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*
# package-lock.json IS committed.

# ---------------------------------------------------------------------------
# Tauri / mobile build output
# ---------------------------------------------------------------------------
src-tauri/target/
src-tauri/gen/schemas/
gen/apple/build/
gen/apple/*.xcworkspace/xcuserdata/
gen/apple/*.xcodeproj/xcuserdata/
gen/apple/Externals/
DerivedData/
*.ipa
*.dSYM.zip
*.xcarchive

# ---------------------------------------------------------------------------
# Application data. Real databases and backups must never be committed.
# ---------------------------------------------------------------------------
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.cairn
*.cairnbackup
backups/

# The one exception, and it is deliberate. The frozen backup fixtures are the only proof
# that a file written by an older build still opens, so they have to be in the repository.
# They hold six invented rows and a password that is printed in the test beside them.
!crates/cairn-db/tests/fixtures/*.cairn

# ---------------------------------------------------------------------------
# Test, coverage and audit artifacts
# ---------------------------------------------------------------------------
coverage/
lcov.info
*.profraw
*.profdata
fuzz/artifacts/
fuzz/corpus/
fuzz/coverage/
mutants.out*/
criterion/
audit-report*.json
sbom*.json

# ---------------------------------------------------------------------------
# OS noise
# ---------------------------------------------------------------------------
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store
._*
*~
