<script lang="ts">
  /**
   * The habits section, which is now the screen of what today asks for.
   *
   * A frame rather than a screen. What was here was the shape of the module drawn with
   * nothing behind it — a year of empty squares and a list that could not be added to — and
   * it did its job: it is what the design was settled against. There is a core behind it
   * now, so it goes, and the year comes back on the detail screen where it belongs to one
   * habit and has real days in it.
   */
  import TodayScreen from '../habits/TodayScreen.svelte';
</script>

<TodayScreen />
