[package]
name = "cairn-domain"
description = "Pure business logic: streaks, budgets, validation and the merge rules."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
cairn-crypto.workspace = true
jiff.workspace = true
thiserror.workspace = true
# Row identifiers. Already in the graph at this exact version through `cairn-db`, and taken from
# the workspace so the sixteen bytes one crate writes are the sixteen the other reads. A draft
# names the folder it goes in, and naming it with a `String` here would mean the check that it is
# an identifier at all happened somewhere else, or nowhere.
uuid.workspace = true

[dev-dependencies]
# Already in the tree for `cairn-crypto`, at the same version. The types in this crate are the
# ones whose invariants are worth stating rather than sampling: a clock that must never go
# backwards and a calendar that must never name a day that does not exist are properties, and a
# handful of examples would agree with a version of either that is wrong in between them.
proptest = "1.11.0"
trybuild = "1.0.121"

[lints]
workspace = true
